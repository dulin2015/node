/**
 * 
 * @param {*} url 请求路径
 * @param {*} data 请求参数
 * @param {*} type 请求类型
 */
async function fetch(url = '', data = {}, type = 'post') {
    if (window.fetch) {

    } else {
        // IE6~11、Edge12~13

    }

}

export default fetch;